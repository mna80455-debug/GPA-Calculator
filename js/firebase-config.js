/**
 * GradeIQ — Firebase Configuration
 * 
 * IMPORTANT: Replace the values below with your actual Firebase project settings.
 * You can find these in the Firebase Console (Project Settings > General).
 */

const firebaseConfig = {
  apiKey: "YOUR_API_KEY",
  authDomain: "YOUR_PROJECT.firebaseapp.com",
  projectId: "YOUR_PROJECT_ID",
  storageBucket: "YOUR_PROJECT.appspot.com",
  messagingSenderId: "YOUR_SENDER_ID",
  appId: "YOUR_APP_ID"
};

// Export config for use in js/firebase.js (module script)
export default firebaseConfig;
